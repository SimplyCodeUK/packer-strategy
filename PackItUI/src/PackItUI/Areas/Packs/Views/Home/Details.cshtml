@model PackItUI.Areas.Packs.Models.PackModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Pack Details";
}
<h2>@ViewData["Title"]</h2>

<form>
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <hr />
        <div class="form-group">
            <label class="control-label col-sm-2" asp-for="Data.PackId"></label>
            <div class="col-sm-10">
                <input class="form-control" asp-for="Data.PackId" disabled="disabled" />
                <span asp-validation-for="Data.PackId"></span>
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-sm-2" asp-for="Data.Name"></label>
            <div class="col-sm-10">
                <input class="form-control" asp-for="Data.Name" disabled="disabled" />
                <span asp-validation-for="Data.Name"></span>
            </div>
        </div>
        <div class="form-group">
            <label class="control-label col-sm-2" asp-for="Data.Notes"></label>
            <div class="col-sm-10">
                <input class="form-control" asp-for="Data.Notes" disabled="disabled" />
                <span asp-validation-for="Data.Notes"></span>
            </div>
        </div>
    </div>
</form>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Data.PackId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
