@model PackItUI.Areas.Packs.Models.HomeViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Packs";

    var DataClass = "control-label";
}
<h2>@ViewData["Title"]</h2>
<table class="table">
    <tr>
        <th scope="col"><label asp-for="Information.Version"></label></th>
        <th scope="col"><label asp-for="Information.About"></label></th>
    </tr>
    <tr>
        <td>@Model.Information.Version</td>
        <td>@Model.Information.About</td>
    </tr>
</table>
@if (Model.Items != null)
{
<table class="table">
    <thead>
        <tr>
            <th scope="col"><label asp-for="Items.FirstOrDefault().PackId"></label></th>
            <th scope="col"><label asp-for="Items.FirstOrDefault().Name"></label></th>
            <th scope="col">
                <a role="button" class="btn btn-primary fa fa-plus" asp-action="Create" />
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Items)
        {
        <tr>
            <td><span class="@DataClass">@item.PackId</span></td>
            <td><span class="@DataClass">@item.Name</span></td>
            <td>
                <a role="button" class="btn btn-info fa fa-pencil-alt" asp-controller="Home" asp-action="Update" asp-route-id="@item.PackId" />
                <a role="button" class="btn btn-primary fa fa-boxes" asp-controller="Home" asp-action="Display" asp-route-id="@item.PackId" />
                <a role="button" class="btn btn-danger fa fa-trash" asp-controller="Home" asp-action="Delete" asp-route-id="@item.PackId" />
            </td>
        </tr>
        }
    </tbody>
</table>
}
