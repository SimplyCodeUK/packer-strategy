/**
 * @copyright Simply Code Ltd. All rights reserved.
 * @license Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
'use strict'

/** Values that represent pattern generators. */
PackIt.Helpers.Enums.PatternGenerator = Object.freeze(
  {
    /** An enum constant representing the split option. */
    Split: 0,

    /** An enum constant representing the split 2 option. */
    Split2: 1,

    /** An enum constant representing the step option. */
    Step: 2,

    /** An enum constant representing the nested option. */
    Nested: 3,

    /** An enum constant representing the around option. */
    Around: 4,

    /** An enum constant representing the odd spiral option. */
    OddSpiral: 5,

    /** An enum constant representing the variable option. */
    Variable: 6,

    /** An enum constant representing the two step option. */
    TwoStep: 7
  }
)
