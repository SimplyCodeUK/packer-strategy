/**
 * @copyright Simply Code Ltd. All rights reserved.
 * @license Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
'use strict'

/** Values that represent how a material is used. */
PackIt.Helpers.Enums.UsageType = Object.freeze(
  {
    /** An enum constant representing the outer option. */
    Outer: 0,

    /** An enum constant representing the base layer option. */
    BaseLayer: 1,

    /** An enum constant representing the inter layer option. */
    InterLayer: 2,

    /** An enum constant representing the top layer option. */
    TopLayer: 3,

    /** An enum constant representing the other inserts option. */
    OtherInserts: 4,

    /** An enum constant representing the trimmings option. */
    Trimmings: 5
  }
)
